# Fixed tasks

Add one directory or markdown file per task. First targets (from policy traffic):

1. `implement.standard` — small PR with tests
2. `review.audit` — known-buggy diff, must find N issues
3. `fanout.explore` — repo map question with exact answer set

Oracles must be deterministic. Prefer exit codes + file existence over LLM judges.
