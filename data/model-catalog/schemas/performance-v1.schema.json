{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/tnunamak/minnows/data/model-catalog/schemas/performance-v1.schema.json",
  "title": "model-catalog performance document v1",
  "description": "Sparse, source-backed quality/effort claims and optional numeric scores. Missing data is expressed via missing[] — never invent chart points. Prefer source_ids[] (document) + source_id (row) pointing at SOURCES.json.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "id",
    "schema_version",
    "kind",
    "provider",
    "retrieved_at",
    "source_urls"
  ],
  "properties": {
    "$schema": {
      "type": "string"
    },
    "id": {
      "type": "string",
      "pattern": "^[a-z][a-z0-9-]+$"
    },
    "schema_version": {
      "type": "integer",
      "const": 1
    },
    "kind": {
      "type": "string",
      "const": "performance",
      "description": "Document kind (unified). Older free-form kinds are invalid under v1."
    },
    "provider": {
      "type": "string",
      "enum": [
        "anthropic",
        "openai",
        "xai",
        "google",
        "alibaba",
        "deepseek",
        "moonshot",
        "minimax",
        "meta",
        "mistral",
        "other"
      ],
      "description": "Vendor that published the claims/scores"
    },
    "retrieved_at": {
      "type": "string",
      "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}$"
    },
    "source_urls": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "string",
        "minLength": 8
      }
    },
    "notes": {
      "type": "string"
    },
    "claims": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "models",
          "statement"
        ],
        "properties": {
          "models": {
            "type": "array",
            "minItems": 1,
            "items": {
              "type": "string",
              "minLength": 1
            }
          },
          "task_families": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "axes": {
            "type": "array",
            "items": {
              "type": "string",
              "enum": [
                "quality",
                "cost",
                "effort",
                "speed",
                "latency",
                "tokens"
              ]
            }
          },
          "statement": {
            "type": "string",
            "minLength": 1
          },
          "implication": {
            "type": "string"
          },
          "source_id": {
            "type": "string",
            "pattern": "^[a-z][a-z0-9-]+$",
            "description": "Primary SOURCES.json id for this claim"
          }
        }
      }
    },
    "scores": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "model",
          "metric",
          "score",
          "unit"
        ],
        "properties": {
          "model": {
            "type": "string",
            "minLength": 1
          },
          "metric": {
            "type": "string",
            "minLength": 1
          },
          "score": {
            "type": "number"
          },
          "unit": {
            "type": "string",
            "enum": [
              "accuracy",
              "pass_rate",
              "elo",
              "other",
              "error_rate"
            ]
          },
          "effort": {
            "type": [
              "string",
              "null"
            ],
            "description": "Effort/reasoning level if applicable (e.g. high, xhigh)"
          },
          "comparisons": {
            "type": "object",
            "additionalProperties": {
              "type": "number"
            },
            "description": "Other model id → score on the same metric (vendor table)"
          },
          "caveat": {
            "type": "string"
          },
          "source_id": {
            "type": "string",
            "pattern": "^[a-z][a-z0-9-]+$",
            "description": "Primary SOURCES.json id for this score row"
          },
          "harness": {
            "type": "string",
            "description": "Eval harness (e.g. claude-code, terminus-2, codex-cli, anthropic-internal)"
          },
          "mode": {
            "type": "string",
            "description": "standard | fast | ultra | multi_agent | …"
          },
          "task_family": {
            "type": "string",
            "description": "Coarse task family for joining to policy ops"
          },
          "source_type": {
            "type": "string",
            "enum": [
              "vendor_table",
              "third_party_board",
              "digitized_chart",
              "local_eval",
              "other"
            ]
          },
          "evidence_grade": {
            "type": "string",
            "enum": [
              "A",
              "B",
              "C",
              "D"
            ],
            "description": "A local/reproduced · B independent third-party · C vendor-reported · D digitized/estimated"
          },
          "observed_at": {
            "type": "string",
            "pattern": "^\\d{4}-\\d{2}-\\d{2}$"
          },
          "effort_verbatim": {
            "type": [
              "string",
              "null"
            ],
            "description": "Source's original effort label if normalized"
          },
          "cost": {
            "type": "object",
            "additionalProperties": false,
            "required": [
              "value",
              "unit"
            ],
            "properties": {
              "value": {
                "type": "number"
              },
              "unit": {
                "type": "string",
                "enum": [
                  "usd_per_task",
                  "usd_per_1m_tokens",
                  "credits_per_task",
                  "other"
                ]
              },
              "basis": {
                "type": "string",
                "enum": [
                  "api_usd",
                  "codex_credits",
                  "other"
                ]
              }
            }
          },
          "confidence": {
            "type": "object",
            "additionalProperties": false,
            "properties": {
              "type": {
                "type": "string"
              },
              "value": {
                "type": "number"
              }
            }
          },
          "metric_id": {
            "type": "string",
            "description": "Stable id in metrics.json when present"
          },
          "comparability_group": {
            "type": "string",
            "description": "Rows may only be compared when this key matches AND comparable is not false"
          },
          "snapshot_id": {
            "type": "string",
            "description": "Stable identity of the source board snapshot, independent of file or ingestion lane."
          },
          "n": {
            "type": "integer",
            "minimum": 0,
            "description": "Sample size stated by the source."
          },
          "ci_lo": {
            "type": "number",
            "minimum": 0,
            "maximum": 1,
            "description": "Lower confidence interval bound as stated by the source."
          },
          "ci_hi": {
            "type": "number",
            "minimum": 0,
            "maximum": 1,
            "description": "Upper confidence interval bound as stated by the source."
          },
          "pass_at_4": {
            "type": "number",
            "minimum": 0,
            "maximum": 1,
            "description": "Pass@4 score as stated by the source."
          },
          "comparable": {
            "type": "boolean",
            "description": "If false, never cross-rank with other rows even with same metric_id"
          }
        }
      }
    },
    "missing": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Explicit gaps (e.g. non-digitized chart series)"
    },
    "source_ids": {
      "type": "array",
      "minItems": 0,
      "items": {
        "type": "string",
        "pattern": "^[a-z][a-z0-9-]+$"
      },
      "description": "Ids from SOURCES.json that back this document"
    },
    "run": {
      "type": "object",
      "description": "Optional local-eval run receipt (not a quality fact)",
      "additionalProperties": true
    }
  },
  "anyOf": [
    {
      "required": [
        "claims"
      ],
      "properties": {
        "claims": {
          "minItems": 1
        }
      }
    },
    {
      "required": [
        "scores"
      ],
      "properties": {
        "scores": {
          "minItems": 1
        }
      }
    }
  ]
}
