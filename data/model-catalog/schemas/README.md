# model-catalog JSON Schemas

| File | Purpose |
|------|---------|
| `pricing-v1.schema.json` | API USD / Codex credits tables |
| `performance-v1.schema.json` | Claims + scores (+ optional `source_ids` / row `source_id`) |
| `sources-v1.schema.json` | Pack-level provenance registry (`SOURCES.json`) |
| `pack-v1.schema.json` | Copy of pack envelope (also under `data/schemas/`) |

Human contract: [../SCHEMA.md](../SCHEMA.md). Registry: [../SOURCES.json](../SOURCES.json).
