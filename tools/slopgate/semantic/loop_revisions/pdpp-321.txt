The migration removed the reference implementation, leaving the reference-contract package writing OpenAPI and route documents into a deleted directory. Publishing stopped because those files could not be tracked. The change keeps the package’s pure contract builders, removes their obsolete disk writers and generation commands, and adds a retired-output guard that fails if any of five deleted-tree artifacts return. The data-connect repository is now the sole canonical home for those artifacts, preventing a stale local writer from creating a competing copy. It also removes a release-policy requirement for the deleted command-line and read-core packages; the missing command made every release-quality run fail. The local policy check and release guide now describe the remaining workflow.

Independent review found that the generated Biome exception ledger still described the retired OpenAPI output. Its stored count and fingerprint no longer matched its records, so the policy guard detected drift. The corresponding Biome exclusion was already gone; I removed the obsolete ledger record and recomputed its count and fingerprint from the remaining records. A failing-to-passing ledger consistency proof first rejects the reviewed version, then verifies that the repair has a matching count and fingerprint and no retired-output reference. I reran the retired-output guard and the specification-copy comparison; they prove that no local artifact returns and that the checked specification copies agree. I did not run GitHub Actions locally.

Assisted-by: AI