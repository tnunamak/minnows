The migration removed the reference implementation, leaving the reference-contract package writing OpenAPI and route documents to a deleted directory. Publishing stopped because those files could not be tracked. The change keeps the package’s pure contract builders, removes obsolete disk writers and generation commands, and adds a retired-output guard. This guard fails if any of five deleted-tree artifacts return, preventing a stale local writer from creating a competing copy since the data-connect repository is now the sole canonical home. It also removes a release-policy requirement for the deleted command-line and read-core packages; the missing command had made every release-quality run fail. The local policy check and release guide now describe the remaining workflow.

I ran each local semantic-release command: dependency installs pinned to the lockfile, generated-artifact drift validation, the retired-output guard, specification-copy comparison, released-package script tests, and dist-tag classifier tests. I also ran the reference-contract verification suite (152 tests) and recreated a retired artifact to confirm the guard rejects it. I did not run GitHub Actions because checkout, package-manager setup, and runtime setup are hosted-only steps.

## Round 2

Independent review found that the generated Biome exception ledger still described the retired OpenAPI output. Its stored count and fingerprint no longer matched its records, so the policy guard detected drift. The corresponding Biome exclusion was already gone; I removed the obsolete ledger record and recomputed its count and fingerprint from the remaining records. A failing-to-passing ledger consistency proof first rejects the reviewed version, then verifies that the repair has a matching count and fingerprint and no retired-output reference. I reran the retired-output guard and the specification-copy comparison to prove that no local artifact returns and that the checked specification copies agree. I did not run the hosted continuous-integration platform locally.

Assisted-by: AI