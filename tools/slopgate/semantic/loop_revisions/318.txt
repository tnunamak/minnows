A release from `main` stopped before publishing because semantic-release could not render notes. `conventional-changelog-conventionalcommits` 10.4.0 requires `conventional-changelog-writer` 9 or newer, but `@semantic-release/release-notes-generator` and `@semantic-release/commit-analyzer` declare `^8.0.0`. pnpm resolved writer 8.4.0, causing the preset to fail with its “requires ... writer@9 or newer” error.

This adds `conventional-changelog-writer: 9.2.1` to `pnpm-workspace.yaml`. The lockfile now records `conventional-changelog-conventionalcommits` 10.4.0 and writer 9.2.1 shared by both semantic-release modules. It also deletes `SEMREL-REPORT.md` while leaving `SEMREL-318-REPORT.md` untracked.

`npm view` confirmed semantic-release 25.0.9, release-notes-generator 14.1.1, commit-analyzer 13.0.1, conventional-changelog-conventionalcommits 10.4.0, and writer 9.2.1. `pnpm why conventional-changelog-writer` shows writer 9.2.1 shared by both modules.

An isolated local clone ran semantic-release’s dry-run against a one-commit fixture after a local `v99.0.0` tag. It classified the commit as a patch release, completed the `generateNotes` step, and generated a 99.0.1 “Bug Fixes” section. This proves the note-generation path works with writer 9.2.1, but it does not exercise the hosted GitHub Actions workflow.

Dependency installation, artifact comparison, specification parity, publication-label policy, and whitespace validation pass. The reference-contract generation, release-policy, and Ultracite checks still fail for existing files outside this change; `SEMREL-318-REPORT.md` records their exact causes.

Assisted-by: AI