A release from `main` failed because `conventional-changelog-conventionalcommits` 10.4.0 requires `conventional-changelog-writer` 9 or newer, but pnpm resolved 8.4.0. This pins `conventional-changelog-writer` to 9.2.1 in `pnpm-workspace.yaml` and deletes `SEMREL-REPORT.md`. `npm view` and `pnpm why` confirm the new resolution. A local dry-run against a `v99.0.0` tag generated a 99.0.1 patch release, proving note generation works, though the GitHub Actions workflow remains unverified. Existing failures in reference-contract, release-policy, and Ultracite checks persist and are documented in `SEMREL-318-REPORT.md`.