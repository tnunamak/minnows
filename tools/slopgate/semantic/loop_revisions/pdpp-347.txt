## Why

Two code moves landed without doc updates, leaving newcomers with failing commands and 404 links. `reference-implementation/` moved to `PDP-Connect/data-connect` in #284, and connector source now begins in `PDP-Connect/data-connectors`. This repository's copy under `packages/polyfill-connectors/connectors/` is frozen against direct edits.

| File | Stale claim | Effect on a newcomer |
|---|---|---|
| `README.md` section "Reference implementation" | Four `pnpm` scripts to run from the repository root; two links to `reference-implementation/README.md` | Four consecutive `ERR_PNPM_NO_SCRIPT` failures; both links are 404s |
| `CONTRIBUTING.md` intro | Invites connector and reference-implementation code here | Contributor edits the wrong repository; a connector change fails the write-freeze guard with no earlier warning |
| `CONTRIBUTING.md` closing paragraph | Same dead `reference-implementation/README.md` link | 404 |
| `docs/reference/connector-authoring-guide.md` | Describes this repository as the place to write a connector | Same wrong-repository outcome |
| `.github/workflows/polyfill-connectors.yml:32` | Justifies the freeze by citing `DUALHOME-AUDIT.md` | That file is not in this repository on any branch, so the freeze cites nothing |

## What changed

`README.md` points to `PDP-Connect/data-connect` for the reference implementation and drops the four dead commands. It links the local collector runbook for running collection on the reader's own machine.

`CONTRIBUTING.md` names both repositories holding code and gives each kind its path in `data-connect`: reference-implementation code under `reference-implementation/`, console code under `apps/console/`. It names the write-freeze guard and its `connector-write-freeze-override` escape hatch, so contributors learn the rule before CI enforces it. The dead closing link is repointed.

`docs/reference/connector-authoring-guide.md` states that connector source begins in `data-connectors` and that authoring rules apply in both repositories.

`.github/workflows/polyfill-connectors.yml` states the freeze reason without citing the missing file. The `~150 consuming files` figure is unverified and dropped. The edit is comment-only; guard behavior is unchanged.

## What did not change

No code, workflow step, or guard behavior changed. The write-freeze guard rejects and accepts exactly what it did before. The frozen connector copy under `packages/polyfill-connectors/connectors/` stays in place.

## Verification

```
pnpm spec:check
  → spec:check passed (9 canonical pairs, 3 site-only extensions, 1 reference-only root spec)
  → git status: only the 4 intended files modified (no generated drift)

git grep -n "](reference-implementation/" -- '*.md'
  → (no output — no dead relative markdown link remains; bare-text mentions are not covered by this pattern)

python3 -c "yaml.safe_load(open('.github/workflows/polyfill-connectors.yml'))"
  → YAML OK — jobs: write-freeze-guard, verify

curl -o /dev/null -w '%{http_code}' -L <each new link>
  → 200  https://github.com/PDP-Connect/data-connect
  → 200  https://github.com/PDP-Connect/data-connectors
  → 200  https://github.com/PDP-Connect/data-connect/blob/main/reference-implementation/README.md

git ls-tree -d --name-only origin/main apps/console reference-implementation   # in a data-connect checkout
  → apps/console
  → reference-implementation
```

The `curl` results prove each new link returns 200, replacing previous 404s. The `git ls-tree` output confirms the named directories exist on `data-connect`'s `main`. `spec:check` passes, confirming documentation edits did not desynchronize generated site pages.

## Left alone on purpose

Two nearby claims are not defects here. The local collector runbook correctly documents existing Slack and Signal connectors, verified by `git log --all --diff-filter=A` showing Signal added in `1c4a0964ab`. The self-host page has linked `docs/operator/local-collector-runbook.md` since `849ced2477` (2026-08-07) at `apps/site/src/app/(concept)/self-host/page.tsx:130`; an older deployed copy is a deployment lag, not a content defect. The broken `@pdpp/local-collector` package (pdpp#266) is out of scope.

Assisted-by: AI