The migration that removed this repository’s former reference implementation left the reference-contract package, the code that defines the reference API, writing OpenAPI (the standard HTTP-interface description format) and route documents (descriptions of available API paths) into a deleted directory. Publishing therefore stopped because those newly written files could not be tracked. The change keeps the package’s pure contract builders, removes their obsolete disk writers and generation commands, and adds a retired-output guard, a check that fails if any of five deleted-tree artifacts return. The separate data-connect repository is now the sole canonical home for those artifacts, so this prevents a stale local writer from creating a competing copy. It also removes a release-policy requirement, a check that defined expected release commands, for the deleted command-line and read-core packages; the missing command made every release-quality run fail. The local policy check and release guide now describe the workflow that remains. I ran each local command in semantic-release, the automated publishing tool: both dependency installs pinned to the lockfile’s exact versions, generated-artifact drift validation, the retired-output guard, specification-copy comparison, tests that ensure released packages expose their required scripts, and dist-tag classifier tests that select a package’s publication channel. I also ran the reference-contract verification suite (152 tests) and recreated a retired artifact to confirm that the guard rejects it. I did not run GitHub Actions, the hosted continuous-integration platform, because checkout, package-manager setup, and runtime setup are hosted-only steps.

## Round 2

Independent review found that the generated Biome exception ledger, a JSON Lines inventory of active exceptions to Biome (the repository’s formatter and linter), still described the retired OpenAPI output. Its stored count and fingerprint, the hash that identifies the complete record set, no longer matched its records, so the policy guard detected drift. The corresponding Biome exclusion, a configuration rule that tells the formatter not to process a path, was already gone; I removed the obsolete ledger record and recomputed its count and fingerprint from the remaining records. A failing-to-passing ledger consistency proof first rejects the reviewed version, then verifies that the repair has a matching count and fingerprint and no retired-output reference. I reran the retired-output guard and the specification-copy comparison; they prove that no local artifact returns and that the checked specification copies agree. I did not run the hosted continuous-integration platform locally.

Assisted-by: AI
